# Workspace Setup

## Workspace Root

Open only:

`D:\solomiya-landing-mvp`

This is the source-of-truth workspace.

## Do Not Open As Main Workspace

Do not open this folder as the primary workspace:

`C:\Users\Andriy\Downloads\solomiya_netlify\netlify_deploy`

It is a deploy/export snapshot, not source.

## VS Code / Cursor / Claude / Codex

- Use `D:\solomiya-landing-mvp` as the project root.
- Keep deploy/export copy read-only.
- Read governance docs before changes.
- Do not mix source work with Downloads artifacts.
- Do not save secrets into the workspace.

## Recommended Workspace Name

`solomiya-energy.code-workspace`

This file can be created later if VS Code workspace settings are needed. Do not create editor settings until requirements are clear.

## Startup Checklist

- Confirm root is `D:\solomiya-landing-mvp`.
- Read `ai/ai-session-start.md`.
- Read `project-rules.md`.
- Check `git status --short`.
- Confirm task scope.
- Avoid production files unless explicitly authorized.

